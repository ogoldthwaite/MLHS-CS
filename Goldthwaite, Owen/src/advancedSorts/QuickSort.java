package advancedSorts;

import java.util.Comparator;

public class QuickSort
{
    public static <T> void sort(T[] a, Comparator<T> c, int start, int end)
    {
        
    }
    
    public static <T> int partition(T[] a, Comparator<T> c, int start, int end)
    {
        
    }

    private static void swap(Object[] a, int i, int j)
    {
        Object b = a[i];
        a[i] = a[j];
        a[j] = b;
    }
}
